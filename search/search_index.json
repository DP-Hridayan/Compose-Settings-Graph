{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Compose Settings Graph","text":"<p>A declarative, type-safe DSL for constructing strictly reactive settings screens in Jetpack Compose. </p> <p>Unlike traditional implementations that recompose lists upon state changes, Compose Settings Graph utilizes an internally hoisted layout strategy. Values, interaction states, and themes update locally at the item level. The DSL graph is evaluated dynamically, ensuring minimal recomposition overhead while supporting real-time visibility conditioning and global search indexing.</p>"},{"location":"#installation","title":"Installation","text":"<p>Declare the dependency in your module's <code>build.gradle.kts</code>:</p> <pre><code>dependencies {\n    implementation(\"io.github.dp-hridayan:compose-settings-graph:1.1.0\")\n}\n</code></pre>"},{"location":"#quick-start","title":"Quick Start","text":"<p>Initialize the global routing state, and construct the UI using the <code>SettingsColumn</code> DSL.</p> <pre><code>val graphState = rememberSettingsGraphState {\n    onBooleanChanged { key, newValue -&gt; prefs.putBoolean(key.toString(), newValue) }\n    onIntChanged { key, newValue -&gt; prefs.putInt(key.toString(), newValue) }\n    isChecked { key -&gt; prefs.getBoolean(key.toString(), false) }\n    selectedValue { key -&gt; prefs.getInt(key.toString(), 0) }\n}\n\nCompositionLocalProvider(LocalSettingGraphState provides graphState) {\n    SettingsColumn(modifier = Modifier.fillMaxSize()) {\n        group(\"Application Theme\") {\n            radioGroupItem(\"theme_mode\") {\n                options(\n                    RadioButtonOption(0, \"System Default\"),\n                    RadioButtonOption(1, \"Light\"),\n                    RadioButtonOption(2, \"Dark\")\n                )\n            }\n        }\n    }\n}\n</code></pre>"},{"location":"advanced_features/","title":"Advanced Features","text":""},{"location":"advanced_features/#dynamic-property-evaluation","title":"Dynamic Property Evaluation","text":"<p>The DSL evaluates block conditions at runtime, preventing the need for complex reactive architecture when updating UI states.</p>"},{"location":"advanced_features/#visibility","title":"Visibility","text":"<p>To dynamically insert or remove an item based on remote configurations or contextual flags, pass a pure evaluation lambda to <code>visible</code>. The background parsing engine excludes disabled items from the global search index entirely.</p> <pre><code>clickableItem(SettingsKeys.CloudModels) {\n    title(R.string.ai_models)\n    icon(Icons.Outlined.AutoAwesome)\n    visible { FeatureConfig.isAiEnabled }\n    onClick { navController.navigate(NavRoutes.AiModelsScreen) }\n}\n</code></pre>"},{"location":"advanced_features/#state-enabling","title":"State Enabling","text":"<p>Control interaction access based on dependent states. Disabled items automatically apply a generic <code>0.5f</code> alpha layout overlay and intercept touch events.</p> <pre><code>switchItem(SettingsKeys.AdvancedAnalytics) {\n    title(R.string.advanced_analytics)\n    enabled { FeatureConfig.isTelemetryActive }\n}\n</code></pre>"},{"location":"advanced_features/#manual-event-overrides","title":"Manual Event Overrides","text":"<p>By default, the <code>SwitchItem</code> and <code>SwitchBannerItem</code> delegate their touch interactions exclusively to the global <code>onBooleanChanged</code> callback. </p> <p>However, you can specify an <code>onClick</code> override on the item to assume total manual control over the interaction event. When an <code>onClick</code> block is present on a switch node, both the row click and the switch thumb click will execute the override, bypassing the global change emitter.</p> <pre><code>switchItem(SettingsKeys.DevMode) {\n    title(\"Developer Mode\")\n    onClick {\n        // Evaluate condition before mutating state\n        if (!hasSufficientPermissions()) {\n            dialogManager.show(WarningDialog)\n        } else {\n            // Trigger manual state mutation\n            settingsViewModel.setBoolean(SettingsKeys.DevMode, true)\n        }\n    }\n}\n</code></pre>"},{"location":"advanced_features/#badges","title":"Badges","text":"<p>Render localized visual indicators for specific configurations using <code>experimentalFlagText</code>.</p> <pre><code>switchItem(SettingsKeys.BetaRenderer) {\n    title(\"Use Vulkun Engine\")\n    experimentalFlagText(\"Beta\")\n}\n</code></pre>"},{"location":"core_concepts/","title":"Core Concepts","text":""},{"location":"core_concepts/#architecture-philosophy","title":"Architecture Philosophy","text":"<p>The library is designed to separate the declaration of the settings UI from the underlying storage mechanism. This eliminates the structural overhead of passing ViewModels, DataStores, or <code>SharedPreferences</code> instances deeply into composable hierarchies. </p>"},{"location":"core_concepts/#global-state-resolution","title":"Global State Resolution","text":"<p>State synchronization is managed through a tiered callback architecture. The <code>LocalSettingGraphState</code> CompositionLocal provides the base layer for resolving states and intercepting mutation requests.</p> <p>Using <code>rememberSettingsGraphState</code>, you supply the discrete implementation for data extraction and persistence. The library is agnostic to the storage layer, seamlessly supporting Android Jetpack DataStore, Room, or standard <code>SharedPreferences</code>.</p> <pre><code>val prefs by settingsViewModel.preferences.collectAsState(initial = emptyPreferences())\n\nval settingsGraphState = rememberSettingsGraphState {\n    onBooleanChanged { key, newValue -&gt;\n        val sk = key as? SettingsKeys&lt;*&gt; ?: return@onBooleanChanged\n        settingsViewModel.setBoolean(sk as SettingsKeys&lt;Boolean&gt;, newValue)\n    }\n\n    onIntChanged { key, newValue -&gt;\n        val sk = key as? SettingsKeys&lt;*&gt; ?: return@onIntChanged\n        settingsViewModel.setInt(sk as SettingsKeys&lt;Int&gt;, newValue)\n    }\n\n    isChecked { key -&gt;\n        val sk = key as? SettingsKeys&lt;*&gt; ?: return@isChecked false\n        prefs[booleanPreferencesKey(sk.name)] ?: (sk.defaultValue as Boolean)\n    }\n\n    selectedValue { key -&gt;\n        val sk = key as? SettingsKeys&lt;*&gt; ?: return@selectedValue -1\n        prefs[intPreferencesKey(sk.name)] ?: (sk.defaultValue as Int)\n    }\n}\n</code></pre>"},{"location":"core_concepts/#type-safe-identifier-keys","title":"Type-Safe Identifier Keys","text":"<p>To prevent runtime reference errors from raw strings, the architecture supports generic type keys. The <code>key</code> parameter across all item builders is <code>Any</code>. This permits the definition of restricted sealed classes or enums for the settings registry.</p> <pre><code>sealed class SettingsKeys&lt;out T&gt;(val name: String, val defaultValue: T) {\n    object SmoothScrolling : SettingsKeys&lt;Boolean&gt;(\"smooth_scrolling\", true)\n    object TerminalFontStyle : SettingsKeys&lt;Int&gt;(\"terminal_font\", 0)\n}\n</code></pre> <p>When building the graph, the static objects are passed securely to the builder blocks:</p> <pre><code>SettingsColumn {\n    group(\"Terminal Behavior\") {\n        switchItem(SettingsKeys.SmoothScrolling) {\n            title(R.string.smooth_scrolling)\n        }\n    }\n}\n</code></pre>"},{"location":"customization/","title":"Customization","text":"<p>For use-cases that exceed standard list configurations, the DSL exposes <code>item</code> block injection for rendering pure <code>Composable</code> structures directly within the hierarchical column stream. </p> <p>This enables the integration of custom spacer constraints, complex visual headers, or specialized layout controls that maintain standard <code>SettingsColumn</code> scroll positioning.</p>"},{"location":"customization/#custom-composables","title":"Custom Composables","text":"<p>You can inject any external Composable directly into the node list. An arbitrary <code>key</code> string must be provided for the <code>LazyColumn</code> reconciliation engine.</p> <pre><code>SettingsColumn(\n    modifier = Modifier.fillMaxWidth()\n) {\n    // Top Spacer\n    item(key = \"spacer_top\") {\n        Spacer(\n            modifier = Modifier\n                .fillMaxWidth()\n                .height(25.dp)\n        )\n    }\n\n    group(R.string.file_actions) {\n        switchItem(SettingsKeys.SaveWholeOutput) {\n            title(R.string.save_whole_output)\n        }\n    }\n}\n</code></pre>"},{"location":"customization/#architectural-complex-layouts","title":"Architectural Complex Layouts","text":"<p>Custom nodes can manage complex nested animations and graphic rendering without compromising the strict declarative nature of the DSL tree. </p> <p>Below is a production example of a specialized <code>SpinningGears</code> and custom header implementation:</p> <pre><code>SettingsColumn(...) {\n    item(\"HEADER_ANIMATION\") {\n        Box(\n            modifier = Modifier\n                .fillMaxWidth()\n                .heightIn(300.dp)\n        ) {\n            FloatingIconsBackground(...)\n\n            Column(modifier = Modifier.fillMaxWidth()) {\n                Box(\n                    modifier = Modifier.fillMaxWidth(),\n                    contentAlignment = Alignment.Center,\n                ) {\n                    SpinningGears(modifier = Modifier.size(175.dp))\n                }\n\n                AutoResizeableText(\n                    text = stringResource(R.string.settings),\n                    fontWeight = FontWeight.Black,\n                    style = MaterialTheme.typography.displayLargeEmphasized,\n                )\n            }\n        }\n    }\n\n    group {\n        clickableItem(SettingsKeys.Behavior) { ... }\n    }\n}\n</code></pre>"},{"location":"customization/#scroll-behavior-integration","title":"Scroll Behavior Integration","text":"<p>The <code>SettingsColumn</code> is fully compatible with Material 3 nested scrolling protocols. Pass a <code>TopAppBarState</code> instance directly into the root configuration to synchronize list displacement vectors with custom collapsed app bars.</p> <pre><code>val scrollBehavior = TopAppBarDefaults.pinnedScrollBehavior(rememberTopAppBarState())\n\nScaffold(\n    modifier = Modifier.nestedScroll(scrollBehavior.nestedScrollConnection),\n    topBar = { TopAppBar(...) }\n) { paddingValues -&gt;\n    SettingsColumn(\n        modifier = Modifier,\n        contentPadding = paddingValues,\n        topAppBarState = scrollBehavior.state,\n        hapticsEnabled = true,\n    ) {\n        // ...\n    }\n}\n</code></pre>"},{"location":"item_types/","title":"Item Types","text":"<p>The DSL provides distinct builder blocks for rendering common UI components. All builders support property configurations for titles, descriptions, and leading icons. Text nodes can be supplied as raw strings or localized XML resource IDs (<code>@StringRes</code>, <code>@DrawableRes</code>).</p>"},{"location":"item_types/#groups","title":"Groups","text":"<p><code>group</code> acts as the structural container for all child nodes. Items within a group are visually structured inside adjacent bounded cards. The group accepts an optional header string or string resource.</p> <pre><code>group(R.string.local_adb_shell) {\n    // Child items\n}\n</code></pre>"},{"location":"item_types/#interactive-elements","title":"Interactive Elements","text":""},{"location":"item_types/#clickable-item","title":"Clickable Item","text":"<p>Renders a standard row intended for navigation or discrete action triggering.</p> <pre><code>clickableItem(SettingsKeys.OutputSaveDirectory) {\n    title(R.string.configure_save_directory)\n    description(R.string.des_configure_save_directory)\n    icon(R.drawable.ic_directory)\n    onClick { dialogManager.show(SettingsDialogKey.ConfigureSaveDir) }\n}\n</code></pre>"},{"location":"item_types/#switch-item","title":"Switch Item","text":"<p>Renders a standard row with a trailing <code>Switch</code> composable.</p> <pre><code>switchItem(SettingsKeys.DisableSoftKeyboard) {\n    title(R.string.disable_softkey)\n    description(R.string.des_disable_softkey)\n    icon(R.drawable.ic_disable_keyboard)\n}\n</code></pre>"},{"location":"item_types/#switch-banner","title":"Switch Banner","text":"<p>Renders a high-emphasis, pill-shaped banner designed for primary feature toggles. Modifies the underlying background color scheme to <code>primaryContainer</code>.</p> <pre><code>switchBannerItem(SettingsKeys.MasterFeatureToggle) {\n    title(\"Enable advanced tools\")\n}\n</code></pre>"},{"location":"item_types/#selection-elements","title":"Selection Elements","text":""},{"location":"item_types/#radio-group","title":"Radio Group","text":"<p>Renders a vertically stacked list of cards containing <code>RadioButton</code> instances. The system tracks internal selection through the active <code>selectedValue</code>.</p> <pre><code>radioGroupItem(SettingsKeys.LocalAdbWorkingMode) {\n    options(\n        RadioButtonOption(0, R.string.mode_standard),\n        RadioButtonOption(1, R.string.mode_legacy),\n        RadioButtonOption(2, R.string.mode_compatibility)\n    )\n}\n</code></pre>"},{"location":"item_types/#button-group","title":"Button Group","text":"<p>Renders a segmented horizontal control layout using interconnected <code>ToggleButton</code> instances. Useful for compact selection enumerations.</p> <pre><code>buttonGroupItem(SettingsKeys.TerminalFontStyle) {\n    options(\n        ButtonGroupOption(TerminalFontStyle.MONOSPACE, R.string.monospace),\n        ButtonGroupOption(TerminalFontStyle.SYSTEM_FONT, R.string.system_font),\n    )\n}\n</code></pre>"}]}